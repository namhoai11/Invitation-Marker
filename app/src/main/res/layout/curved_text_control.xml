<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:paddingHorizontal="20dp"
    android:paddingVertical="16dp">

    <!-- Curved Text Label and Value -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Curved Text"
            android:textSize="16sp"
            android:textColor="#000000" />

        <TextView
            android:id="@+id/curved_text_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0°"
            android:textSize="16sp"
            android:textColor="#000000"
            android:textStyle="bold"
            android:minWidth="40dp"
            android:gravity="end" />
    </LinearLayout>

    <!-- SeekBar with - and + buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btn_decrease_curve"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_minus"
            android:contentDescription="Decrease curve"
            android:layout_marginEnd="12dp" />

        <SeekBar
            android:id="@+id/seekbar_curved_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:max="200"
            android:progress="100"
            android:progressTint="#4CAF50"
            android:thumbTint="#4CAF50" />

        <ImageButton
            android:id="@+id/btn_increase_curve"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_plus"
            android:contentDescription="Increase curve"
            android:layout_marginStart="12dp" />
    </LinearLayout>
</LinearLayout>